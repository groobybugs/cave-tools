---
description: Toggle cave-tools level (off | hint | enforce | strict)
---
Activate cave-tools mode: $ARGUMENTS

If no level given, use default (enforce). If "off", deactivate.

Prefer cave__* MCP tools over built-in read/grep/glob/bash/edit/write.
Switch: off | hint | enforce | strict.
Behavior persists until session ends or user says "stop cave-tools".
